<p id="notice"><%= notice %></p>

<p>
  <strong>Username:</strong>
  <%= @user.username %>
</p>

<p>
  <strong>Email:</strong>
  <%= ofuscate_email_address(@user.email) %>
</p>

<%= render '/shared/posts_table', title: 'My Posts', posts: @user.posts %>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
